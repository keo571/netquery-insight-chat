# ================================
# DEPLOYMENT MODES
# ================================
#
# 1. DEVELOPMENT MODE (npm start on port 3000)
#    - Frontend runs on http://localhost:3000
#    - Calls backend(s) using URLs below
#    - Database switching enabled via UI
#
# 2. PRODUCTION MODE (served by backend)
#    - Frontend served by backend at http://server:8000
#    - Uses window.location.origin for API calls (same-origin)
#    - No configuration needed - just access the backend URL
#    - Database determined by which backend served the page
#
# ================================

# Backend URLs (only used in development mode)
# In production, frontend auto-detects the backend URL
REACT_APP_SAMPLE_URL=http://localhost:8000
REACT_APP_NEILA_URL=http://localhost:8001

# Force development mode even when not on port 3000 (optional)
# REACT_APP_DEV_MODE=true

# UI customization
REACT_APP_AGENT_NAME=Netquery
REACT_APP_WELCOME_TITLE=Welcome to Netquery!
REACT_APP_WELCOME_MESSAGE=Ask me questions about your network infrastructure data.
REACT_APP_INPUT_PLACEHOLDER=Ask about your infrastructure data...
